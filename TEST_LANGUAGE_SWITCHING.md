# 语言切换测试指南

## 已修复的问题

### 1. ✅ 语言切换现在影响整个应用
**修复方式：**
- 添加了 `PassthroughSubject` 来触发 Observable 更新
- 使用 `Binding` 直接绑定到 LocalizationManager.language
- 所有视图都会实时响应语言变化

### 2. ✅ 登录/退出功能正常工作
**修复方式：**
- 模拟器显示"Simulator Test Sign In"测试按钮
- 真实设备使用 Sign in with Apple
- 退出登录会触发动画过渡返回登录界面
- 添加了状态调试日志

### 3. ✅ 语言选项正确显示
- 中文 = Chinese (中文)
- English = English (English)

## 测试步骤

### 测试1: 登录功能
1. **启动应用** - 应该看到登录界面
2. **点击 "Simulator Test Sign In"** 按钮
3. ✅ **验证**: 应该进入主界面，看到5个Tab

### 测试2: 语言切换
1. **点击最右边的 Settings (设置) Tab**
2. **看到语言选择器**，默认显示 "English | 中文"
3. **点击 "中文"** 切换到中文
4. ✅ **验证以下变化**:
   - Settings → 设置
   - Language → 语言
   - Edit Profile → 编辑资料
   - Body Metrics → 身体数据
   - Sign Out → 退出登录

5. **切换到其他Tab查看**:
   - Tab 1: Workout → 训练
   - Tab 2: Plan → 计划
   - Tab 3: Exercises → 动作库
   - Tab 4: Plans → 训练计划

6. **进入 Exercises (动作库) Tab**
7. ✅ **验证**:
   - 标题变为 "动作库"
   - 筛选器显示中文: 全部、胸部、背部、肩部等
   - 动作名称只显示中文（如："杠铃卧推" 而不是 "Barbell Bench Press 杠铃卧推"）
   - 预设动作 → 显示中文
   - 自定义动作 → 显示中文

8. **切换回 English**
9. ✅ **验证所有界面恢复英文**

### 测试3: 视频链接优先级
1. **进入 Exercises → 选择任意动作**
2. **在英文模式下**:
   - ✅ YouTube 链接显示在上方
   - ✅ Bilibili 链接显示在下方

3. **切换到中文模式**:
   - ✅ Bilibili 链接显示在上方
   - ✅ YouTube 链接显示在下方

### 测试4: 退出登录
1. **在 Settings 界面**
2. **点击 "退出登录" (Sign Out) 按钮**
3. ✅ **验证**:
   - 应用平滑过渡回登录界面
   - 控制台显示: "🔐 Sign out button tapped"
   - 控制台显示: "🔐 Signing out..."
   - 控制台显示: "🔐 Sign out completed. isAuthenticated = false"

### 测试5: 重新登录
1. **点击 "Simulator Test Sign In"**
2. ✅ **验证**:
   - 返回主界面
   - 语言设置保持之前的选择

## 命令行测试脚本

```bash
# 清理并重新安装
cd /Users/christopher/heyuxuan_prjs/Xcode/WorkOutNow
xcodebuild -scheme WorkOutNow -configuration Debug \
  -destination 'platform=iOS Simulator,name=iPhone 17' clean build

# 启动模拟器
xcrun simctl boot 1C9ACAC6-9A50-41B4-97FA-95379C4DA8E4
open -a Simulator

# 安装应用
APP_PATH="$(find ~/Library/Developer/Xcode/DerivedData/WorkOutNow*/Build/Products/Debug-iphonesimulator -name "WorkOutNow.app" | head -1)"
xcrun simctl install 1C9ACAC6-9A50-41B4-97FA-95379C4DA8E4 "$APP_PATH"

# 启动应用
xcrun simctl launch 1C9ACAC6-9A50-41B4-97FA-95379C4DA8E4 Christopher.WorkOutNow
```

## 控制台日志说明

正常工作时应该看到：
```
🔐 Simulator detected - bypassing authentication
🌐 Language changing from en to zh
🌐 Language changed, new value: zh
🔐 Sign out button tapped
🔐 Signing out...
🔐 Sign out completed. isAuthenticated = false
🔐 Authentication state changed: false
```

## 已知限制

1. **Sign in with Apple 仅在真机或 TestFlight 可用**
   - 模拟器必须使用测试按钮

2. **CloudKit 当前已禁用**
   - 需要为真机构建时重新启用
   - 需要将所有模型属性改为可选或设置默认值

3. **语言切换不会触发应用重启**
   - 所有界面实时更新
   - 某些系统组件（如日期格式）可能仍使用系统语言

## 故障排除

### 问题：语言切换不生效
**解决方案：** 检查控制台是否有 "🌐 Language changing" 日志

### 问题：退出登录不工作
**解决方案：** 检查控制台是否有 "🔐 Sign out" 日志

### 问题：应用启动崩溃
**解决方案：**
```bash
# 查看崩溃日志
ls -lt ~/Library/Logs/DiagnosticReports/WorkOutNow* | head -1
```

## 成功标准

- ✅ 登录界面显示正确（包含模拟器测试按钮）
- ✅ 可以成功登录
- ✅ 语言切换影响所有界面
- ✅ Tab 标签实时更新
- ✅ 动作名称只显示选定语言
- ✅ 视频链接顺序根据语言改变
- ✅ 退出登录返回登录界面
- ✅ 重新登录保持语言设置
